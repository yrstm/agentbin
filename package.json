{
  "name": "agentbin",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "Share coding-agent output and collect short-lived reviewer feedback.",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/unit.test.js && node test/integration.mjs"
  },
  "devDependencies": {
    "wrangler": "4.121.0"
  }
}
